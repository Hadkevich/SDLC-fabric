{
  "spec_version": "v1",
  "workflow_id": "96fa0a8f-a908-4773-9363-15c3fc120443",
  "task_id": "T-06-REVIEW",
  "reviewer": "reviewer-agent (Claude Sonnet)",
  "verdict": "approved_with_comments",
  "reviewed_at": "2026-06-27T10:00:00Z",
  "summary": "All 13 acceptance criteria (AC1–AC13) are satisfied. BLK-001 has been resolved: GET /teams/{team_id}/risk-summary now returns HTTP 200 with a TeamRiskSummary payload using a Phase 1 fallback (all DeveloperProfile records, no Team entity yet). AC8 is fully functional — Manager Dashboard renders per-developer risk badges without raw behavioral vectors. Additional fixes applied: server-side logout clears HttpOnly refresh cookie (NBI-005), COUNT query in get_developer_matches is O(1) and respects min_score filter (NBI-001/002), embedding background task now writes DeveloperEmbedding rows and updates embedding_status (NBI-003), api-contracts.json weight examples corrected (NBI-006), Admin Weight Config tab added to Manager UI. Security posture strong: bcrypt, JWT type-claim validation, role-based access, HttpOnly cookie, startup guard. Remaining non-blocking items: team_rejection_rate analytics still unscoped (NBI-004, deferred to Phase 2 when Team entity exists), no team risk summary test (NBI-007, deferred).",
  "blocking_issues": [],
  "non_blocking_issues": [
    {
      "id": "NBI-001",
      "category": "performance",
      "description": "In src/api/developers.py get_developer_matches(), the total record count is computed by loading ALL matching MatchRecord rows into memory via scalars().all() (two full ORM hydrations), then counting the resulting Python list. For a developer with thousands of match records this serialises all rows into memory just to get a count. A SELECT COUNT(*) via func.count() would be O(1) in the DB instead of O(n) in Python.",
      "file_ref": "src/api/developers.py",
      "line_ref": 401,
      "suggestion": "Replace the first SELECT MatchRecord query with: total_result = await db.execute(select(func.count()).where(MatchRecord.developer_id == developer_id)); total = int(total_result.scalar()). Import func from sqlalchemy."
    },
    {
      "id": "NBI-002",
      "category": "correctness",
      "description": "In get_developer_matches(), the total count query (line 401–404) does not apply the min_score filter even when min_score is provided. The returned total field therefore reflects all match records for the developer, not only those above the min_score threshold. API consumers that paginate using min_score + top_k will receive a total that overstates the actual result set size.",
      "file_ref": "src/api/developers.py",
      "line_ref": 398,
      "suggestion": "Build a separate count_query starting from the same base filter as query (including min_score), then use that for the COUNT(*). Alternatively make total represent the full unfiltered count and document it clearly in the OpenAPI description (which does say 'before top_k filtering' but does not mention min_score filtering)."
    },
    {
      "id": "NBI-003",
      "category": "correctness",
      "description": "The _enqueue_embeddings() background task in src/api/developers.py calls generate_developer_embeddings() which returns vector dictionaries but never writes them to the developer_embeddings table and never updates DeveloperProfile.embedding_status from 'pending' to 'ready'. As a result, every developer profile created or updated via the API retains embedding_status='pending' indefinitely. The matching engine sets behavioral_data_unavailable=True for such profiles. Note: actual 5-dimension scoring in POST /matches is computed from inline request vectors (not from stored embeddings), so match scores are numerically correct. The gap is that ANN-based candidate retrieval always operates in degraded mode and the embedding_status field is misleading.",
      "file_ref": "src/api/developers.py",
      "line_ref": 127,
      "suggestion": "The background task should (a) call db.execute() to upsert DeveloperEmbedding rows into developer_embeddings for each embedding type, and (b) update DeveloperProfile.embedding_status to 'ready' on success or 'failed' after exhausted retries. This requires passing a DB session or connection URL into the background task, similar to the pattern used in _async_generate_explanation() in matches.py."
    },
    {
      "id": "NBI-004",
      "category": "correctness",
      "description": "GET /analytics/team-rejection-rate?team_id={id} in src/api/analytics.py (line 147) queries SELECT DISTINCT developer_id FROM feedback_records without any team_id filter. Because there is no Team entity or TeamMembership table in Phase 1, the endpoint ignores team_id entirely and returns rejection statistics for every developer who has submitted feedback in the entire system. The returned team_id field reflects the requested UUID, but the members list is unscoped. A manager viewing team 'A' and team 'B' receives identical results.",
      "file_ref": "src/api/analytics.py",
      "line_ref": 147,
      "suggestion": "Either (a) add a 501 Not Implemented response with a clear comment (consistent with the approach taken for team risk summary) until Team membership is modelled, or (b) return only the developer_ids that are members of the requested team by joining with a team membership table once that entity exists."
    },
    {
      "id": "NBI-005",
      "category": "security",
      "description": "The logout flow in frontend/src/App.tsx and src/api/client.ts clears the in-memory access_token but does not call any server-side endpoint to invalidate the HttpOnly refresh_token cookie. After the user signs out, the refresh_token cookie remains valid on the browser until its 7-day TTL expires. If the cookie is already in a cookie-theft scenario, the attacker can use it for up to 7 days post-logout. This is documented as a known limitation in code_spec.json, and is an acceptable Phase 1 trade-off given the stateless JWT design, but it should be tracked for resolution before production.",
      "file_ref": "frontend/src/App.tsx",
      "line_ref": 269,
      "suggestion": "Add a DELETE /auth/logout endpoint that deletes or overwrites the HttpOnly refresh_token cookie server-side (set max_age=0) and maintains a server-side revocation list if zero-TTL is insufficient. Phase 1 minimum: at logout, POST to /auth/logout so the server can overwrite the cookie with an expired one."
    },
    {
      "id": "NBI-006",
      "category": "maintainability",
      "description": "The default weights in the api-contracts.json MatchResponse example (w4=0.10, w5=0.15) differ from the actual code defaults in src/api/config.py and src/api/matches.py (w4_timezone=0.15, w5_growth=0.10) and from the test_plan.json TC-001 and TC-062 values (w4=0.15, w5=0.10). OpenAPI examples are non-normative, but the discrepancy creates confusion when developers validate their integration against the docs example.",
      "file_ref": "artifacts/api-contracts.json",
      "line_ref": 620,
      "suggestion": "Update the MatchResponse and WeightConfig example in api-contracts.json to use w4=0.15, w5=0.10 to match the actual seeded defaults. The contract constraint ('weights sum to 1.0') is not violated by either set; it is purely a documentation accuracy fix."
    },
    {
      "id": "NBI-007",
      "category": "test_coverage",
      "description": "test_plan.json TC-072 ('Manager risk endpoint returns badges without raw behavioral vectors') tests GET /api/v1/developers/{id}/risk (individual developer endpoint) — not GET /teams/{team_id}/risk-summary. There is no test case that exercises the team risk summary endpoint or documents its current 501 status. Additionally, no test verifies that the ExplanationCache entries are properly expired after PUT /config/weights (the implicit invalidation via cache_key hash change is untested).",
      "file_ref": "artifacts/test_plan.json",
      "line_ref": 688,
      "suggestion": "Add a test case mapping to AC8 that calls GET /teams/{team_id}/risk-summary and asserts the response shape matches TeamRiskSummary once the endpoint is implemented. Add a weight-change-then-rematch test that verifies the new score uses updated weights and the old ExplanationCache entry is not returned."
    }
  ]
}
