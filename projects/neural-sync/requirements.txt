# NEURAL SYNC — Python dependency pinning
# Install: pip install -r requirements.txt
# Python: >=3.11

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # Form / file upload support
apscheduler==3.10.4              # Optional continuous re-optimization loop (NEURAL_SYNC_REOPT_INTERVAL)

# ── Database (async) ─────────────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0                  # Async PostgreSQL driver (runtime)
psycopg2-binary==2.9.9           # Sync driver used by Alembic migrations only
pgvector==0.2.5                  # pgvector SQLAlchemy / psycopg2 integration

# ── Alembic migrations ───────────────────────────────────────────────────────
alembic==1.13.1

# ── Auth ────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0 # JWT (HS256)
bcrypt==5.0.0                    # Password hashing (direct bcrypt; passlib 1.7.4 is incompatible with bcrypt 5.x)

# ── LLM client (Google Gemini — free tier) ───────────────────────────────────
google-genai==1.2.0             # Free Gemini API key: https://aistudio.google.com/app/apikey
httpx==0.27.0                    # HTTP client (also used by the FastAPI test client)

# ── Pydantic (FastAPI data validation) ───────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0

# ── Embeddings (optional backends) ───────────────────────────────────────────
# Default embedding dim is 1536 (pgvector columns + HNSW indexes + seed). The
# random + OpenAI backends emit 1536; sentence-transformers emits 384 and is only
# selected when EMBEDDING_DIM=384 is set explicitly (see src/engine/embeddings.py).
# It is large (~900 MB) and OFF by default to keep the 1536 path canonical and CI lean.
# Uncomment ONLY together with EMBEDDING_DIM=384 + a column re-migration.
# sentence-transformers==2.7.0
openai==1.30.1                   # OpenAI embedding fallback (1536-dim)

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1             # .env file loading for local dev
tzdata==2024.1                   # IANA timezone database (zoneinfo backend)
numpy==1.26.4                    # Numerical operations for embeddings

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                    # Async test client for FastAPI
pytest-cov==5.0.0
